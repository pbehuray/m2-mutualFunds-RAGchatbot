# Streamlit secrets — DO NOT commit real values
# Configure these in the Streamlit Community Cloud dashboard instead.

api_base_url = "http://localhost:8002"

# GROQ_API_KEY = "your-groq-api-key-here"
